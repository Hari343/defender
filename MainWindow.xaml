<Window x:Class="WpfApp2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Height="850"
        Width="1050"
        WindowStartupLocation="CenterScreen"
        Background="Transparent"
        WindowStyle="None"
        AllowsTransparency="True"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        Loaded="Window_Loaded">

    <Window.Resources>

        <Style TargetType="Button">
            <Setter Property="Background" Value="#0078D7"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="Width" Value="170"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#00A2FF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#005A9E"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#2B2B3D"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="SelectionBrush" Value="#0078D7"/>
            <Setter Property="BorderBrush" Value="#55556A"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="FontSize" Value="17"/>
            <Setter Property="Margin" Value="0,0,30,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style TargetType="Label">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="17"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#2B2B3D"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#55556A"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="MinHeight" Value="40"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="8"
                                    Padding="0">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0"
                                               Text="{TemplateBinding SelectionBoxItem}"
                                               Margin="8,0,0,0"
                                               VerticalAlignment="Center"
                                               Foreground="{TemplateBinding Foreground}"
                                               FontSize="{TemplateBinding FontSize}"/>
                                    <ToggleButton Grid.Column="1"
                                                  x:Name="ToggleButton"
                                                  IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                  Background="Transparent"
                                                  Foreground="White"
                                                  Content="▼"
                                                  Width="35"
                                                  Margin="0,0,5,0"
                                                  VerticalAlignment="Center"
                                                  BorderThickness="0"
                                                  IsTabStop="False"
                                                  Focusable="False"/>
                                </Grid>
                            </Border>
                            <Popup x:Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Slide"
                                   MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">
                                <Border Background="#2B2B3D"
                                        BorderBrush="#55556A"
                                        BorderThickness="1"
                                        CornerRadius="8">
                                    <ScrollViewer>
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ToggleButton" Property="Background" Value="#3A3A4D"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="Background" Value="#2B2B3D"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="Padding" Value="8"/>
                        <Setter Property="Height" Value="35"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ComboBoxItem">
                                    <Border Background="{TemplateBinding Background}"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#0078D7"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="#005A9E"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <Border Background="#1E1E2F" CornerRadius="16">
        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Defender "
                       FontSize="30"
                       FontWeight="Bold"
                       Foreground="White"
                       Margin="0,0,0,25"/>

            <TextBlock Text="0000 0000 0001 1011" Foreground="#FF8F8F99" VerticalAlignment="Center" Margin="0,0,12,0" HorizontalAlignment="Right" />

            <Grid Grid.Row="1" Margin="0,15,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Row 0, Column 0: Log File Location -->
                <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,15,0">
                    <Label Content="Log File Location:" Margin="0,0,0,8"/>
                    <Border Background="#2B2B3D" BorderBrush="#55556A" BorderThickness="1" CornerRadius="8" Padding="0">
                        <TextBox x:Name="txtLogLocation"
                                 Text="C:\Logs\defender_log.txt"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 Padding="8"
                                 MinHeight="40"/>
                    </Border>
                </StackPanel>

                <!-- Row 0, Column 1: Wire Shark Location -->
                <StackPanel Grid.Row="0" Grid.Column="1" Margin="15,0,0,0">
                    <Label Content="Wire Shark Location:" Margin="0,0,0,8"/>
                    <Border Background="#2B2B3D" BorderBrush="#55556A" BorderThickness="1" CornerRadius="8" Padding="0">
                        <TextBox x:Name="txtWireSharkLocation"
                                 Text="C:\Program Files\Wireshark\"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 Padding="8"
                                 MinHeight="40"/>
                    </Border>
                </StackPanel>

                <!-- Row 1, Column 0: Pcap Location -->
                <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,15,15,0">
                    <Label Content="Pcap Location:" Margin="0,0,0,8"/>
                    <Border Background="#2B2B3D" BorderBrush="#55556A" BorderThickness="1" CornerRadius="8" Padding="0">
                        <TextBox x:Name="txtPcapLocation"
                                 Text="C:\Pcap\capture.pcap"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 Padding="8"
                                 MinHeight="40"/>
                    </Border>
                </StackPanel>

                <!-- Row 1, Column 1: Network Interface -->
                <StackPanel Grid.Row="1" Grid.Column="1" Margin="15,15,0,0">
                    <Label Content="Network Interface:" Margin="0,0,0,8"/>
                    <ComboBox x:Name="cmbNetworkInterface"
                              SelectedIndex="0">
                        <ComboBoxItem>Select Interface</ComboBoxItem>
                        <ComboBoxItem>Ethernet</ComboBoxItem>
                        <ComboBoxItem>WiFi</ComboBoxItem>
                        <ComboBoxItem>Loopback</ComboBoxItem>
                    </ComboBox>
                </StackPanel>
            </Grid>

            <Grid Grid.Row="2" Margin="0,20,0,20">
                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center">
                    <CheckBox x:Name="chkRDP" Content="RDP"/>
                    <CheckBox x:Name="chkSSH" Content="SSH"/>
                    <CheckBox x:Name="chkTelnet" Content="Telnet"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Right">
                    <Button x:Name="btnStart" Content="Start" Click="StartButton_Click"/>
                    <Button Content="Open Wireshark" Click="Start_Wireshark"/>
                    <Button Content="Exit" Click="CloseButton_Click"/>
                </StackPanel>
            </Grid>

            <Border Grid.Row="3"
                    Background="#11111C"
                    CornerRadius="10"
                    BorderBrush="#3A3A4D"
                    BorderThickness="1"
                    Padding="15">

                <TextBox x:Name="logText"
                         Background="#11111C"
                         Foreground="#00FF66"
                         BorderThickness="0"
                         FontSize="16"
                         TextWrapping="Wrap"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         IsReadOnly="True"
                         VerticalAlignment="Top"
                         Text="Log messages will appear here..."/>
            </Border>
        </Grid>
    </Border>
</Window>